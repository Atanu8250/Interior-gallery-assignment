/**
 * Purpose: Stores request validation rules for image module payloads.
 * How it is used: Applied in routes or middleware before controllers run.
 * Continue here: DTO schemas, query validation, and error mapping.
 */
export {};
